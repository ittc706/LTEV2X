* 修改传给GTT模块中的数据结构，改成每辆车的干扰列表，而不传对应Pattern的干扰列表，因为有些干扰根本不用计算
* 增加delete的封装形式
* 内存还有泄漏
* 将RSU VeUE类内数据结构改成继承体系
* 将配置文件分为系统级以及各个单元
* GTT_HighSpeed中计算干扰信道的方法没有改
* TDM_DRA和RR模块中的干扰列表没有改
* 判断是否需要重新计算MCS的条件没有改